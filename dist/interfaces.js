"use strict";
/*interface Game{
    title: string;
    description: string;
    genre: string;
    platform?: string[];
    getSimilars?: (title: string) => void;
    
    }
    
    const tlou: Game = {
    title: "The Last of Usk",
    description: "The best game in the world",
    genre: "action",
    platform: ["PS3","PS4"],
    getSimilars: (title:string) => {
       console.log(`Similar games to ${title}: Uncharted, Far Cry and Dayzone`);

    }
    
    }
    console.log(tlou.title);
    if(tlou.getSimilars){
    tlou.getSimilars(tlou.title);
    }
    console.log(tlou.genre);

    interface DLC extends Game{
        originalGame: Game;
        newContent: string[];
    }

    const leftbehind: DLC = {
        title: "The last of us - Last Behind",
        description: "A story that addresses Joel's past before he meet ellie ",
        genre:"action",
        platform: ["PS4" , "XBOX360"],
        originalGame: tlou,
        newContent: ["3 hours Story", "new caracteres"]
    }

  class CreateGame  implements Game{
        title: string;
        description: string;
        genre: string;
      
        constructor(t: string, d: string, g: string){
            this.title = t;
            this. description = d;
            this.genre = g;
        }
    }
*/
